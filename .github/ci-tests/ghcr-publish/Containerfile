FROM alpine:3.21

LABEL org.opencontainers.image.title="org-infra-test"
LABEL org.opencontainers.image.description="Test image for reusable_publish_ghcr.yml workflow"

# Minimal test payload
RUN echo "org-infra test image" > /test-marker

USER nobody

CMD ["cat", "/test-marker"]
